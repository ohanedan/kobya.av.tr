---
import { emphasize } from '../lib/text';

interface Props {
  lines: string[];
}

const { lines } = Astro.props;
---

{
  lines.map((line) => (
    <span class="line">
      <span class="line-inner" set:html={emphasize(line)} />
    </span>
  ))
}
